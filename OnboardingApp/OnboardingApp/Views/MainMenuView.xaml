<UserControl x:Class="OnboardingApp.Views.MainMenuView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:hc="https://handyorg.github.io/handycontrol"
      xmlns:s="https://github.com/canton7/Stylet"
      xmlns:local="clr-namespace:OnboardingApp.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <hc:ScrollViewer>
            <hc:WaterfallPanel VerticalAlignment="Center"  Groups="{Binding Value,ElementName=UpDownGroups}" hc:PanelElement.FluidMoveBehavior="{StaticResource BehaviorXY200}">
               
                <hc:WaterfallPanel.Resources>
                    <Style TargetType="Border">
                        <Setter Property="RenderTransformOrigin" Value="0.3,0.3" />
                        <Setter Property="RenderTransform">
                            <Setter.Value>
                                <ScaleTransform ScaleX="1" ScaleY="1" />
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)"
                                                 To="1.05" Duration="0:0:0.0" />
                                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)"
                                                 To="1.05" Duration="0:0:0.0" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)"
                                                 To="1" Duration="0:0:0.0" />
                                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)"
                                                 To="1" Duration="0:0:0.0" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </hc:WaterfallPanel.Resources>
                
                <Border Height="200" Background="{DynamicResource PrimaryBrush}" Effect="{StaticResource EffectShadow1}" Margin="5">
                    <Button Height="200" Width="350" Content="Карта офиса" Cursor="Hand" BorderBrush="Transparent" Background="Transparent" FontSize="20" Command="{s:Action GoToOfficeMapCommand}"/>
                </Border>
                <Border Height="140" Background="{DynamicResource DangerBrush}" Effect="{StaticResource EffectShadow1}" Margin="5">
                    <Button Height="140" Width="350" Content="Список сотрудников" Cursor="Hand" BorderBrush="Transparent" Background="Transparent" FontSize="20" Command="{s:Action GoToEmployersCommand}"/>
                </Border>
                <Border Height="100" Background="{DynamicResource SuccessBrush}" Effect="{StaticResource EffectShadow1}" Width="387">
                    <Button Height="100" Width="388" Content="База знаний" Cursor="Hand" BorderBrush="Transparent" Background="Transparent" FontSize="20" Command="{s:Action GoToKnowlegeBaseCommand}"/>
                </Border>
                <Border Height="170" Background="{DynamicResource InfoBrush}" Effect="{StaticResource EffectShadow1}" Margin="5">
                    <Button Height="170" Width="350" Content="Текущие задачи" Cursor="Hand" BorderBrush="Transparent" Background="Transparent" FontSize="20" Command="{s:Action GoToTasksCommand}"/>
                </Border>
            </hc:WaterfallPanel>
        </hc:ScrollViewer>
    </Grid>
</UserControl>
